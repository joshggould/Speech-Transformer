torch
torchaudio
datasets>=2.14.0
huggingface_hub>=0.20.0
tokenizers
torchmetrics
tqdm
tensorboard
# datasets>=4 decodes audio via torchcodec, which needs FFmpeg *shared* libs
# on PATH (Windows: winget install Gyan.FFmpeg.Shared)
torchcodec

# Phase A: desktop inference (transcribe_mic.py)
soundfile
sounddevice

# Phase B: chunking + server (server.py); system dep: ffmpeg for m4a uploads
silero-vad
fastapi
uvicorn[standard]
python-multipart
